# Function points

The traceability matrix for the rewrite: every function point has an id
and at least one test whose name contains that id. It is filled in as the
milestones land, and a later milestone adds the test that reads this file
and fails when an id has no test.
